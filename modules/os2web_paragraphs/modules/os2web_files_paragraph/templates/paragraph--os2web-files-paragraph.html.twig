{% set classes = [
  'paragraph',
  'paragraph--type--' ~ paragraph.bundle|clean_class,
  view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
  paragraph.isPublished() ? null : 'paragraph--unpublished'
] %}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}

      {# Begin - heading #}
      {{ content.field_os2web_files_heading }}
      {# End - heading #}

      {# Begin - files #}
      {{ content.field_os2web_files_files }}
      {# End - files #}

    {% endblock %}
  </div>
{% endblock paragraph %}
