{% set classes = [
  'paragraph',
  'paragraph--type--' ~ paragraph.bundle|clean_class,
  view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
  not paragraph.isPublished() ? 'paragraph--unpublished'
] %}
{% block paragraph %}
  <div{{ attributes.addClass(classes) }}>
    {% block content %}

      {# Begin - heading #}
      {{ content.field_os2web_iframe_heading }}
      {# End - heading #}

      {# Begin - subheading #}
      {{ content.field_os2web_iframe_subheading }}
      {# End - subheading #}

      {# Begin - code #}
      {{ content.field_os2web_iframe_code }}
      {# End - code #}

    {% endblock %}
  </div>
{% endblock paragraph %}
